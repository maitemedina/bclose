
enum VerifyAutentication {
validate,
login,
register
}

enum SocialNetwork {
  focebook,
  google,
  apple,
}


enum ErrorRequest {
  Internet,
  TimeOut,
  ServerError,
}

enum DevicesConnect{
  OXIMETRO,
  BALANCA,
  THERMOMETER,
  GRAPHC,
  BPM,
  NULL
}

